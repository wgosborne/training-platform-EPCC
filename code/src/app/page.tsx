export default function Home() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>Running Trainer MVP</h1>
      <p>API endpoints are available at <code>/api/plans</code></p>
      <p>
        See <code>.env.example</code> for database configuration.
      </p>
    </main>
  );
}
